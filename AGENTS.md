# 数据交换工具宣传片：设计与实现约束

本文件适用于 `promo-video/` 目录及其所有子目录，是项目根目录规则的补充；如有冲突，以项目根目录规则和用户当次明确要求为准。

## 1. 项目目标

- 产品名称统一使用“数据交换工具”。
- 视频用于“一门户六中心”的产品宣传页面，目标是让用户快速理解产品的主要能力，而不是承担完整培训或操作手册的职责。
- 成片采用 1920 × 1080、60 FPS。旁白、配乐和 Logo 由用户后期添加，Remotion 工程默认不内置这些内容。
- 视觉表达应像宣传片，不做成 PPT：每个镜头只传达一个主要结论，并形成一个明确视觉焦点。

## 2. Apple 风格的使用边界

“Apple 风格”仅指借鉴清晰层级、克制留白、精确排版、一致材质和有目的的动效，不代表复制 Apple 的具体页面。

- 不使用 Apple Logo、产品图、商标、广告文案或其他品牌资产。
- 不逐像素照搬 Apple 官网布局、镜头或组件。
- 不在项目中分发未获授权的 Apple 字体文件。优先使用系统无衬线字体栈，并为中文提供可靠回退字体。
- 所有画面仍需服务于“数据交换工具”的产品语义，而不是为了像 Apple 而添加无意义装饰。

## 3. 核心设计原则

### 3.1 目的明确

- 每个分镜只能有一个核心结论和一个第一视觉焦点。
- 每个元素都必须帮助用户理解叙事；不能说明产品、状态或关系的元素应删除。
- 画面与旁白相互补充，不要逐字重复旁白，也不要依赖旁白才能看懂关键关系。

### 3.2 简洁

- 优先使用少量大元素、充足留白和渐进呈现。
- 禁止装饰性的英文眉题、重复栏目名、无信息价值的小字、永久悬浮说明条和技术氛围噪点。
- 不要在同一画面同时展示完整拓扑、功能说明、统计指标、状态列表和警告信息；内容过多时拆成连续镜头。
- 最终画面不使用 Emoji 作为正式图标。图标优先使用代码绘制的简洁几何图形或已确认授权的资产。

### 3.3 层级清晰

- 默认信息层级为：核心标题 → 主体对象/关系 → 必要的辅助状态。
- 同一时刻最多保留两个主要文字层级，避免标题、标签、解释和数字同时争夺注意力。
- 标题、主体和连接关系必须先于装饰进入视线。
- 用户应能在约 2 秒内判断当前镜头在讲什么；如果做不到，应删减或拆镜头。

### 3.4 一致与协调

- 全片统一圆角、间距、描边、阴影、字体、色彩语义和动效节奏。
- 相同类型的节点、标签和连接线必须保持一致的视觉编码。
- 相邻镜头优先保持对象连续性，通过移动、聚合、缩放或淡化完成转场，避免无原因地整屏重置。

### 3.5 精确与克制

- 对齐、间距、曲线、字重和动效落点必须准确，禁止文字重叠、线条穿字、元素闪烁和边缘裁切。
- 阴影、玻璃感和渐变只用于建立层级，不作为装饰堆叠。
- 不使用未经证实的客户数量、性能、成功率、效率提升或其他营销数字。

## 4. 视觉系统

### 4.1 色彩

默认使用少量、低噪声的中性色，加一个主强调色：

- 页面背景：`#FBFBFD`
- 主文字：`#1D1D1F`
- 次文字：`#6E6E73`
- 分隔线/弱边框：`#D2D2D7`
- 主强调蓝：`#0071E3`
- 可选辅助紫：`#AF52DE`

约束：

- 单个镜头原则上只使用一个主强调色；渐变只用于一个关键对象或关键状态。
- 红色只表示真实错误或阻断，橙色只表示警告、风险或复杂度上升，不能仅为增加“科技感”而使用。
- 连接线的默认状态应低对比；只有正在叙述的路径才提升亮度、色彩或线宽。
- 禁止大面积彩虹渐变、多种高饱和色竞争，以及无语义的发光效果。

### 4.2 材质与形状

- 卡片圆角建议为 24–36 px，使用低对比描边和柔和分层阴影。
- 半透明和模糊只用于建立前后层级，并确保文字对比度不受影响。
- 不要把所有内容都装进卡片；关系、流向和空间位置本身可以承担信息表达。
- 同类对象使用相同尺寸和样式，核心对象可通过尺度差异形成层级。

### 4.3 排版

- 使用系统无衬线字体栈，例如：`-apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC", "Microsoft YaHei", sans-serif`。
- 正文和标签优先使用 Regular、Medium 或 Semibold；避免 Light、Thin 等影响视频可读性的细字重。
- 1920 × 1080 画面建议：主标题 48–64 px，节点名称 18–24 px，短标签不小于 14 px。只在次要且停留充足时使用 12 px，禁止 9–10 px 的装饰性文字。
- 主标题尽量不超过 14 个汉字；辅助说明尽量不超过 22 个汉字/行，并控制在两行内。
- 非协议名、产品术语或必要状态，不使用全大写英文和增加字距的英文眉题。

### 4.4 布局与安全区

- 1920 × 1080 画面四周至少保留 96 px 安全边距；标题、核心对象和关键标签优先放在中央约 80% 的区域内。
- 门户页面可能存在播放器控件，底部关键信息应额外留出空间。
- 画面可以不对称，但必须维持视觉平衡；避免规整的“标题 + 多卡片网格”长期停留，以免形成 PPT 感。
- 节点数量增加时，应向视觉中心聚合叙事，而不是单纯向四周扩张并制造空洞中心。

## 5. 信息与业务语义

- 必须区分数据格式/业务标准与通信/接入方式：
  - 数据格式或标准：`JSON`、`XML`、`CSV`、`X12`、`EDIFACT`
  - 通信或接入方式：`REST API`、`SFTP`、`AS2`、`Webhook`
- `EDIFACT ORDERS` 等名称的大小写和写法在全片保持一致。
- 业务节点名称应直观，例如 ERP、WMS、OMS、TMS、客户系统、供应商系统、海关/外部平台。
- 演示数据必须为虚构数据，不得出现真实密钥、个人信息、客户敏感信息或内部地址。
- 不在画面中加入无法由产品能力或项目资料支撑的结论。

## 6. 镜头与信息密度

- 使用渐进揭示：先出现主体，再出现连接，再出现格式/协议，最后呈现复杂度或解决结果。
- 一个节拍只发生一个主要变化。新信息出现前，已有次要信息应保持稳定、降低对比或退出。
- 需要用户阅读的最终状态必须有清晰停留：关键标题建议稳定展示至少 0.8–1.2 秒，节点或标签建议至少 0.6 秒。
- 复杂关系优先通过运动路径、聚合、分流和状态变化表达，不用一段小字解释。
- 协议标签和数据格式应随对应连接出现，不要一次性铺满整个画面。
- 字幕由后期统一添加时，画面底部不预置长期字幕胶囊，避免重复占位。

## 7. 动效规范

- 动效必须有目的，用于解释出现、连接、转换、聚合、异常或完成状态。
- 默认采用短促、平滑、可预测的缓入缓出；弹簧效果应低回弹，不能让企业产品显得轻浮。
- 优先使用位置、缩放、透明度、遮罩和路径进度建立连续性。
- 避免持续呼吸、反复闪烁、无休止 Ping、剧烈弹跳和大范围外围运动；这些效果会争夺旁白期间的注意力。
- 不依靠运动本身传递唯一关键信息；动画结束后的静态画面仍应可理解。
- 镜头结束前应完成收束并留出稳定帧，便于后期剪辑和转场。

## 8. Remotion 实现约束

- 动画必须由帧数和组合参数确定，保证重复渲染结果一致。
- 统一复用颜色、字体、圆角、阴影、间距和缓动 token，不在分镜中散落近似但不同的常量。
- SVG 连接线应避免遮挡文字；必要时通过层级、路径或局部遮罩处理，而不是增加不透明底块掩盖问题。
- 组件只为真实复用而抽取，不为形式完整而过度抽象。
- 修改镜头时同时检查前后转场的进入、稳定和退出帧，不能只检查单张中间截图。
- 非用户明确要求，不启动本地开发服务器。

## 9. 修改与验证流程

修改视觉、文案或时长前，先检查对应分镜、时间轴配置和相邻转场。保持改动最小且内聚；若改变时长、结构或正式文案，应同步更新相关脚本或说明文件。

至少执行与改动相称的验证：

1. 运行 `npm run typecheck`。
2. 运行 `npm run build`。
3. 渲染受影响镜头的进入、中段、高潮和退出代表帧，并进行视觉检查。
4. 涉及动画或转场时，渲染受影响片段或成片，检查文字可读性、元素遮挡、节奏和闪帧。
5. 输出视频后使用媒体信息工具核对分辨率、帧率、时长和编码。

完成后必须如实说明实际执行的验证及结果；未执行的项目应明确列为未验证风险。

## 10. 分镜验收清单

每个分镜提交前逐项确认：

- 是否只表达一个主要结论？
- 用户能否在约 2 秒内找到第一视觉焦点并理解主题？
- 是否还有可以删除的解释性小字、装饰性英文或重复标签？
- 关键文字在 1080p 门户播放场景下是否清晰？
- 数据格式与通信协议是否分类正确？
- 是否存在未经证实的数字、客户事实或性能承诺？
- 动效是否解释了关系或状态，而不是纯装饰？
- 进入、中段、高潮、退出以及相邻转场是否都已检查？

## 11. 官方设计参考

- Apple Human Interface Guidelines：<https://developer.apple.com/design/human-interface-guidelines>
- Typography：<https://developer.apple.com/design/human-interface-guidelines/typography>
- Layout：<https://developer.apple.com/design/human-interface-guidelines/layout>
- Motion：<https://developer.apple.com/design/human-interface-guidelines/motion>

以上资料用于提炼原则，不构成复制 Apple 品牌资产或具体页面的授权。
