# 数据交换工具宣传片 V1 脚本与 Shot List

> 版本：V1.0  
> 目标时长：约 92 秒
> 画面规格：1920×1080，60 FPS，16:9  
> 制作方式：真实产品录屏 + Remotion 动画包装（S03 为接入动画；S04–S07 已接入录屏；S08 待补录）
> 主要用途：“一门户六中心”中本产品的宣传页面  
> 音频边界：Remotion 只交付无声画面母版，旁白与配乐由后期剪辑添加  
> 核心叙事：业务痛点 → 数据进入平台 → AI 辅助解析与映射 → 转换验证 → 稳定交换与统一监控

## 1. 制作目标

本片不是产品操作教程，而是一条放置在“一门户六中心”产品宣传页面的软件能力介绍片。用户应当在不阅读长篇产品文档的情况下，通过视频理解产品解决的问题、核心操作链路和主要能力。

网页视频可能默认静音播放，因此画面必须满足“静音也能看懂”：关键能力通过标题、短字幕、状态变化和真实 UI 共同表达，不能依赖旁白补充必要信息。旁白文案仍保留在脚本中，仅作为用户后期剪辑时的配音参考和节奏基准，不进入 Remotion 工程。

观众看完后应形成三个明确认知：

1. 企业数据交换的主要难点来自系统、格式和通信协议差异。
2. 平台能够将接入、解析、映射、转换、执行和监控放在一条可管理链路中。
3. 产品的辨识度是“AI 辅助理解数据并生成可审核的映射与转换结果”，而不是不可解释的全自动黑盒。

## 2. 统一演示案例

全片使用同一笔采购订单贯穿，避免不同镜头之间业务上下文跳变。

- 业务场景：供应商向采购企业发送采购订单。
- 本次录屏源端：SFTP-TEST，SFTP / EDI。
- 源数据：EDIFACT ORDERS。
- 本次录屏目标端：JDBC-ORDERS，JDBC / JSON。
- 目标格式：JSON。
- 重点字段：订单号 PO-123456、交货日期（20240915 → 2024-09-15）及商品明细。
- 演示结果：平台识别源协议，解析字段，给出映射建议，预览 JSON 结果并执行交换任务。

演示数据必须为虚构数据，不包含真实客户名称、联系人、电话、地址、Token、内网地址或数据库凭据。

## 3. 完整时间轴

### S01｜传统点对点集成困局｜00:00–00:08｜8 秒

**形式**：Remotion 动画，复用并精简现有 `Scene01PainPoints`。

**画面**：

- 使用克制的蓝白视觉：白色节点卡片、蓝色状态点和浅蓝连接线，不使用紫色强调。
- 左侧依次出现客户系统、供应商系统、海关 / 外部平台，右侧依次出现 ERP、WMS、OMS、TMS。
- 最初只有少量清晰连接，随后 JSON、XML、CSV、X12、EDIFACT 数据格式随数据包出现。
- REST API、SFTP 作为接入方式与数据格式同时显示；数据包标签使用可读的大字号和较慢的移动速度，不形成常驻信息墙。
- 随着更多系统加入，同一个节点开始连接多个系统，线路逐渐交错并变得复杂。

**旁白**：

> 随着客户、供应商和业务系统不断增加，数据交换也变得越来越复杂。

**屏幕主文案**：

> 连接越多，数据交换越复杂

**转场**：复杂网络停留片刻，下一镜从其中一条 EDIFACT 数据链路切入交换中枢。

**后期声音建议**：低频环境音、数据连接声逐渐叠加，结尾加入短促吸附声。Remotion 母版不包含该音轨。

---

### S02｜从复杂连接到统一链路｜00:08–00:14｜6 秒

**形式**：Remotion 连续镜头。

**画面**：

- 延续第一幕的复杂网络终帧，节点与线路向中央收拢并逐渐降低对比度。
- 画面依次显示“连接，更快。”和“转换，更准确。”，同一时刻只保留一个核心句子。
- `EDIFACT ORDERS` 沿清晰的蓝色链路进入数据交换工具，随后从另一侧输出 `JSON`。
- 中央产品节点平移、收束为蓝色交换节点，与 S03 的位置、尺寸和材质一致。

**旁白**：

> 如何让系统连接更快，让数据转换更准确？试试数据交换工具。

**屏幕文案**：

> 连接，更快。
>
> 转换，更准确。
>
> 数据交换工具

**转场**：保留中央产品节点，直接承接 S03 的连接动画，不展开界面模板。

**后期声音建议**：数据包飞行声、平台接收确认声。Remotion 母版不包含该音轨。

---

### S03｜多种接入方式，连接两端｜00:14–00:29｜15 秒

**形式**：连续连接动画。承接 S02 末尾的蓝色产品节点。

- 0–3 秒：产品节点持续保留，SFTP、API、数据库三种接入方式依次展开，暂不显示连接线。
- 3–7 秒：建立 SFTP 链路，一份文件从外部网络进入企业网络中的交换平台。
- 7–11 秒：依次补齐 API 与数据库链路，各展示一次数据汇入，不循环发送数据包。
- 11–15 秒：沿已建立的链路向目标系统推进，镜头轻推并跟随输出数据，收束后短溶解进入 S04；不恢复总览、不重复发送、不切换到连接器摘要。
- 移除本镜头的文件上传操作、原始报文预览和常驻步骤条。网络区域与数据流为能力示意。
- 建链采用完整连线短淡入，与对应节点强调同步；连线稳定后发送一次数据，避免绘制中的悬空端点。网络区域标题与节点分区布局。
- R01 配置素材留存，不使用含地址、账号的填写过程。

**画面主文案**：不同系统，统一连接。

**旁白参考**：覆盖多种主流传输协议，无论是跨网的 SFTP 文件传输，还是高吞吐的 API 与数据库连接，连接两端只需片刻。

---

### S04｜协议识别与结构解析｜00:29–00:40｜11 秒

**形式**：R03 真实解析画面 + 字段局部特写。

- 0–5 秒：原始 EDI 与解析 JSON 对照，聚焦解析侧。
- 5–11 秒：放大解析结果中的订单结构，用 `PO-123456` 贯穿上下文。
- 使用识别完成后的清晰片段，避开大段鼠标选中文本。

**画面主文案**：从原始报文，到清晰结构。

**旁白参考**：识别 EDIFACT ORDERS 报文，将订单内容展开为可查看的字段结构。

---

### S05｜AI 建议与人工审核｜00:40–00:52｜12 秒

**形式**：R04 映射列表 → 日期转换编辑抽屉 → 确认后列表。

- 0–3 秒：字段映射建议及真实置信度、待处理状态。
- 3–9 秒：聚焦交货日期，查看源路径与日期类型转换，并确认保存。
- 9–12 秒：返回列表，保留其他字段的实际状态。
- 剪去 AI 等待及重复编辑，不通过整体倍速表现虚假的处理时效。

**画面主文案**：AI 给出建议，人来确认规则。

**旁白参考**：AI 给出字段映射建议，技术人员查看源路径、调整转换方式，再确认规则。

**口径**：UI 置信度仅表示建议参考；确认一条规则不等于全部校验通过。

---

### S06｜转换结果预览｜00:52–01:02｜10 秒

**形式**：R04 尾段真实数据切换、局部预览及保存完成。

- 0–4 秒：切到转换后数据。
- 4–8 秒：放大订单号 `PO-123456` 和交货日期 `2024-09-15`。
- 8–10 秒：保存后返回模板列表。
- 不添加“全部校验通过”“零误差”状态；保留实际空值和字段审核状态。

**画面主文案**：运行之前，先看转换结果。

**旁白参考**：对照转换后的关键业务值，检查订单与日期信息，再保存本次配置。

---

### S07｜启动任务与执行结果｜01:02–01:11｜9 秒

**形式**：R05 真实任务启动 → 数据流示意 → 真实执行历史。

- 0–2.5 秒：启动已配置的订单传输任务。
- 2.5–5 秒：单次接收、解析、映射、转换、投递示意，连接 SFTP 源端与 JDBC 目标端。
- 5–9 秒：本次真实执行记录，成功 1、失败 0。
- 数字仅描述这一笔演示执行，不作为整体成功率或性能承诺。

**画面主文案**：规则进入任务，数据开始流转。

**旁白参考**：启动订单传输，让规则进入运行链路，执行结果可以在任务历史中查看。

---

### S08｜任务运行与统一监控｜01:11–01:20｜9 秒

> 待补录：本次保留原占位镜头，不将其模拟指标当作真实录屏。

**形式**：真实录屏 + 数字轻动效。

**真实操作**：

1. 展示传输任务列表中的运行状态。
2. 打开一次执行详情或执行日志。
3. 切到 Dashboard，展示任务趋势、健康状态或异常概览。

**旁白**：

> 任务状态、执行记录、运行健康和异常信息集中呈现，排查不再依赖分散日志。

**屏幕浮层**：

> 任务可追踪 · 异常可定位

**转场**：Dashboard 卡片依次收拢为四张能力卡片。

---

### S09｜平台能力总结｜01:20–01:27｜7 秒

**形式**：Remotion 蓝白极简 Bento 能力卡片动画，以一张 AI 渐变卡作为主视觉焦点。

**能力卡片**：

1. 多协议与多格式接入
2. AI 辅助语义映射
3. 流式数据交换
4. 数据安全与审计追踪
5. 任务运行与异常监控

**画面**：

- 保留五卡 Bento 构图和原有依次进入节奏，AI 映射卡作为唯一高彩主视觉，其余卡片控制色彩与材质对比。
- 卡片使用简洁的代码绘制线性图标，不使用 Emoji 和装饰性英文眉题。
- 每张卡片只保留一个大号能力词、一行能力名称和一行必要说明；辅助文字不小于 15 px，正文说明使用 18 px 左右。
- 协议卡只展示 `EDIFACT`、`X12`、`JSON / XML`、`REST API`、`SFTP` 等必要标签，并明确区分数据标准与接入方式。
- AI 卡以 `ORDERS.QTY → wms.quantity` 表达映射关系，使用“建议可审核”说明人工审核边界，不展示未经验证的置信度或效率数字。
- 删除底部长字幕胶囊，旁白和正式字幕由后期统一添加。

**旁白**：

> 从多协议接入到 AI 辅助映射，再到运行监控、数据安全与审计追踪，平台覆盖数据交换的关键环节。

**屏幕能力词**：

> 多协议接入 · AI 智能映射 · 流式交换 · 数据安全与审计 · 全链路监控

**口径限制**：本镜头不出现未经实测确认的吞吐量、正确率、节省比例或客户数量。

**转场**：五张卡片沿数据流轨迹收拢，形成干净的中央落版区域。

---

### S10｜品牌结尾｜01:27–01:32｜5 秒

**形式**：Remotion 蓝白极简品牌落版，Logo 等品牌资产由后期剪辑添加。

**画面**：

- 延续全片蓝白视觉体系，以浅色背景、中央柔光和一条低强度蓝色数据流完成收束，不再出现复杂业务信息。
- 中央依次稳定呈现产品名称“数据交换工具”、价值主张“让异构系统无缝对话 · 让企业数据自由流动”。
- 下方保留四项短能力标签：全主流协议覆盖、AI 辅助语义映射、毫秒级流式吞吐、数据安全与审计。
- 单一蓝色主按钮显示“立即试用”，不增加第二操作入口。
- 白色填充、深色描边的极简矢量箭头光标从右下方进入，平滑移动至按钮中心；点击前短暂停顿，点击时光标轻微缩小，按钮同步下沉、回弹并出现一次克制的蓝色波纹反馈。
- 点击完成后画面保持稳定，便于后期添加 Logo、网址或二维码。

**动效节奏**：

1. 产品名称、价值主张、能力标签和按钮随落版整体柔和进入。
2. 光标在约 0.75 秒后显现，用约 1.4 秒移动至按钮中心。
3. 到达按钮后停顿约 0.25 秒，再完成一次点击与回弹。
4. 最后约 2 秒不再新增信息，保留稳定品牌记忆点和后期落版空间。

**旁白**：

> AI 驱动，让企业数据连接更简单。

**后期主 Slogan 建议**：

> AI 驱动，让企业数据连接更简单

**后期辅助文案建议**：

> 数据交换工具

**Remotion 边界**：保留产品名称、价值主张、能力标签、按钮和矢量箭头光标；不添加 Logo、品牌字体文件、网址或二维码，并在点击完成后提供稳定的后期落版区间。

## 4. 真实录屏素材与复现

现有原始文件完整保留在 `public/recordings/`；可复现的选段、源时间和裁切坐标见 `src/content/recording-edits.json`，生成脚本为 `scripts/prepare-recordings.py`。

| 素材 | 使用位置 |
|---|---|
| R01_连接器配置.mp4 | 留存，当前未取用含地址与账号的配置过程 |
| R02样例解析-1.mov | 素材保留，当前 S03 不使用 |
| R03协议解析-1.mov | S04 协议解析与订单结构 |
| R04AI映射审核-1.mov | S05 审核；S06 预览及保存 |
| R05传输任务-1.mov | S07 启动与执行历史 |
| 待补录监控素材 | S08，待后续替换 |

原始录屏为 30 FPS；成片与动画保持 60 FPS，录屏通过重复帧适配，不宣称增加了真实运动细节。所有剪辑媒体去除音轨，短选段末尾停帧提供阅读时间，并为溶解转场保留 0.25 秒余量。日期编辑抽屉裁去中部空白，将真实规则区域和真实底部操作条组合展示，保留原有按钮操作。

执行 `npm run prepare:recordings` 生成裁切后的 H.264 媒体到 `public/edited/`。执行 `npm run render:recordings` 生成 57 秒中段预览。默认保持整片 92 秒以及 S01、S02、S09、S10 原有实现。

## 5. Remotion 制作清单

### 可复用

- `LightBackground`
- `LaserConnection`
- `NodeCard`
- `SubtitleBanner`
- 现有 S01 痛点场景的节点与连线系统
- 旧版 `Legacy-StandardProtocols` 协议卡片中的部分视觉元素

### 需要新增

- `DataPacket`：统一的数据包与格式状态组件。
- `Camera`：支持跟随、推近和焦点锁定的镜头容器。
- `BrowserFrame`：承载真实录屏并允许局部裁切与缩放。
- `Highlight`：聚光框、箭头、点击波纹和字段高亮。
- `FieldMappingLine`：字段映射连线与审核状态。
- `FeatureCard`：结尾能力总结卡片。
- `EndingPlate`：蓝白极简结尾定板，包含产品名称、价值主张、能力标签、主按钮、矢量箭头点击反馈和稳定落版区，不含 Logo。
- 集中式 `timeline/timing.ts`：统一管理镜头起止、转场重叠和设计帧。

### 需要调整

- `Scene01PainPoints` 保持 8 秒，改为“业务系统 → 多接口 → 多伙伴”的逐层揭示，并取消报错气泡。
- 现有 `Scene02Breakthrough` 改造成数据包跟随和 UI Morph，不再作为独立静态中枢展示。
- 现有 `Scene03Protocols` 后移或拆分，协议库信息并入 S04/S09，不占用独立介绍镜头。
- 全片避免纯硬切；优先使用数据包、窗口边框、字段和监控记录作为转场共同元素。

## 6. 无声母版、后期声音与字幕规范

- Remotion 工程不引入旁白、配乐或音效文件，输出无声画面母版。
- 脚本中的旁白保留为后期配音参考；建议语速为每分钟约 210～240 个汉字，最终以实际声线试读为准。
- 后期背景音乐应保持克制，前半段有轻微紧张感，平台登场后转为稳定、明亮。
- 后期音效只服务于数据流、扫描、映射确认和转场，不做高频连续提示音。
- Remotion 内嵌的是“能力文案”，不是逐字旁白字幕；每次只突出一个主要结论。
- 如果后期需要逐字字幕，应在剪辑软件中单独制作字幕轨；字幕最多两行，单行建议不超过 22 个汉字。
- 底部字幕进入 16:9 安全区，避免与播放器控制栏重叠。
- 主标题、能力关键词和旁白字幕分层，避免同一时间出现三段长文本。
- 关闭声音观看时，测试人员仍应能够复述“接入、解析、映射、预览、交换、监控”这条主链路。

## 7. 网页发布规格

Remotion 输出的是供后期剪辑使用的无声母版；用户完成旁白和配乐剪辑后，再生成网页发布版。

### 无声画面母版

- 1920×1080、60 FPS。
- H.264 高质量 MP4。
- 不包含空白 AAC 音轨，避免后期误判为已有声音。
- 保留 1～2 秒稳定结尾，便于后期添加 Logo、品牌信息、网址或二维码。

### 网页发布版

- MP4 容器，H.264 视频，AAC 音频，像素格式 `yuv420p`。
- 开启 Fast Start，将 MP4 元数据移动到文件头，支持边下载边播放。
- 建议同时准备 1080P 主版本和 720P 轻量版本，由门户按设备或网络条件选择。
- 在不明显损伤 UI 文字清晰度的前提下控制文件体积；最终上限需与门户团队确认，不在脚本阶段硬编码码率。
- 单独输出一张 16:9 封面图。封面应包含产品名称、核心定位和真实 UI/数据流视觉，不使用纯黑首帧。
- 页面不应强制自动播放有声视频；若使用静音自动播放，应提供明显的播放、暂停、音量和全屏控制。
- 用户开始播放后的前 3 秒内必须出现业务问题或产品定位，避免长 Logo 动画。
- 最终接入门户前，在桌面浏览器、常见笔记本分辨率和移动端页面宽度下检查文字可读性。

## 8. 宣传口径规则

以下能力可作为功能描述，但仍应与实际演示画面保持一致：

- 主流协议与格式接入。
- 协议识别和结构解析。
- AI 辅助字段映射及人工审核。
- 转换结果预览。
- 任务执行、监控、异常处理和审计。

以下数字只有在获得测试报告或正式产品材料确认后才能进入成片：

- 解析正确率百分比。
- 毫秒级或每秒吞吐量。
- 节省人工配置时间的比例。
- 零停机、绝对安全、绝对无损等绝对化表述。
- 客户数量、生产环境处理量和可用性指标。

## 9. 制作里程碑与验收

### M1｜脚本锁定

- 确认源协议、产品名、Slogan 参考文案和旁白口径。
- 确认 11 个镜头及总时长。

### M2｜录屏素材锁定

- R01～R05 全部完成。
- 敏感信息检查通过。
- 每个录屏镜头均有明确入点和出点。

### M3｜无声结构版

- 动画、录屏和转场完整串联。
- 时长控制在 88～95 秒。
- 无黑帧、跳帧、文字裁切和明显硬切。

### M4｜声音版

- 由用户在后期剪辑中完成旁白、音乐、音效和逐字字幕。
- 旁白、音乐、音效和字幕同步。
- 旁白没有被音乐遮挡。
- 静音观看时仍能理解主要内容。

### M5｜最终交付

- 1920×1080、60 FPS、H.264 MP4。
- Remotion 无声画面母版、后期完整音频版、网页 1080P/720P 发布版、视频封面图。
- 必要时再制作 30 秒精简版，用于门户首屏或产品卡片预览。
- 对成片执行时长、音轨、分辨率和关键帧视觉检查。
- 对网页发布版执行 Fast Start、像素格式、文件体积和浏览器播放检查。

## 10. 已确认的制作决策

1. 演示源协议使用 `EDIFACT ORDERS`。
2. 门户没有指定单个视频文件的体积上限；仍按网页观看体验进行合理压缩。
3. 使用普通视频播放方式，不要求静音自动播放。
4. 当前只制作约 92 秒完整版，不制作额外的 30 秒预览版。
5. Remotion 母版不包含旁白、配乐或 Logo，均由用户后期添加。
